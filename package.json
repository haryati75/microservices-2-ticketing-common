{
  "name": "@charityx/common",
  "version": "1.0.2",
  "description": "",
  "type": "module",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del build",
    "build": "npm run clean && tsc",
    "pub": "git add . && git commit -m 'Updates' && npm version patch --no-git-tag-version && npm run build && npm publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "del-cli": "^7.0.0",
    "typescript": "^5.9.3"
  }
}
